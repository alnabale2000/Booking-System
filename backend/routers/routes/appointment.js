const express = require("express");

const {} = require("./../controllers/appointment");

const appointmentRouter = express.Router();

// SellersRouter.post("/sellers", createNewSellerAccount);

module.exports = appointmentRouter;
